import { style } from '@vanilla-extract/css';

const whiteboardContainer = style({
  display: 'flex',
  height: '100%',
  minHeight: '400px',
  position: 'relative',
  width: '100%',
});

export { whiteboardContainer };

