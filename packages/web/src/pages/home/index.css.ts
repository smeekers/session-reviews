import { style } from '@vanilla-extract/css';

const container = style({
  paddingBottom: '2rem',
  paddingTop: '2rem',
});

const header = style({
  alignItems: 'center',
});

export { container, header };
